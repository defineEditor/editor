'use strict';
/*
const BootstrapTable = require('react-bootstrap-table-next');
const React = require('react');
const ReactDOM = require('react-dom');

function buildDatasetTable (mdv) {
    let model = mdv.model;

    const dsData = [{oid: 1, name: 'Test', description: 'Label', class: 'BDS'}];
    const columns = [
        {
            dataField : 'oid',
            text      : 'oid'
        },
        {
            dataField : 'name',
            text      : 'Name'
        }, {
            dataField : 'description',
            text      : 'Description'
        }, {
            dataField : 'class',
            text      : 'Class'
        }];

    ReactDOM.render(
        <BootstrapTable keyField='id' data={ dsData } columns={ columns } />,
        document.getElementById('datasets')
    );
}

module.exports = buildDatasetTable;
*/
