import {
    UI_SELECTTAB,
} from "../constants/action-types";

const initialState = {
    currentTab: 'standards',
};

const ui = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default ui;
